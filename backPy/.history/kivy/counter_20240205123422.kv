<MainWidget>:
    BoxLayout:
        orientation:"vertical"
        pos:30,50
        ToggleButton:
            text:"check"
            on_state: root.quit(self)
            
        Button:
            text:"start"
            pos:250,100
            size:300,40
            disabled: not root.active
            on_press:root.msg()
            
        Label:
            text:root.state